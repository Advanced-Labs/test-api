# test-api

A simple Python Flask API for testing subtree workflows.

## Endpoints
- GET /api/health - Health check
- GET /api/users - List users
- GET /api/users/:id - Get user by ID
- GET /api/items - List items
- GET /api/items/by-owner/:id - Items by owner
